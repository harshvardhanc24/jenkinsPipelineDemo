pipeline {
  agent any
  stages {
    stage('Builds') {
      when {
        changeset "refs/heads/main"
      }
      steps {
        // Build steps
      }
    }
    stage('Test') {
      when {
        branch "develop"
        manual true
      }
      steps {
        // Test steps
      }
    }
  }
}
